require('dotenv').config();

const fs = require('fs');
const token = process.env['QR_BOT_TOKEN'];
const clientId = process.env['CLIENT_ID'];
const guildId = ;

const {} = ;
const {REST} = ;
const {Routes} = ;